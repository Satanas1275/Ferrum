cargo run          # compile + exécute
cargo build         # compile juste (debug)
cargo build --release  # compile optimisé (pour de la vraie perf)
cargo check          # vérifie que ça compile SANS générer de binaire (rapide, utile en boucle)